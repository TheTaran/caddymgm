What's Changed

## Optimizations

- Add CI checks for formatting, static analysis, Go tests with race detection, dependency vulnerabilities, dashboard JavaScript, Compose files, and container health.
- Add a CaddyMGM container health check for reliable service readiness.
- Allow manually dispatched release builds to publish both application and custom Caddy images.
