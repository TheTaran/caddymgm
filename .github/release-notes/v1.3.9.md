What's Changed

## New Features

- Added per-web-host upstream connection timeout controls.
- Added per-web-host upstream response-header wait controls as the managed upstream read timeout.

## Optimizations

- Added validation for positive Go duration values and preserved timeout settings when parsing managed Caddy configuration.
