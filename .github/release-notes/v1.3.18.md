What's Changed

## Optimizations

- Rename inferred public web hosts to External and align Web Hosts table columns consistently.

## Bug Fixes

- Remove per-host Caddy bind directives, which cannot target Docker host addresses; listener exposure is now controlled solely through Docker port publishing.
- Continue consuming legacy managed bind markers so existing configurations are migrated safely on their next synchronization.
