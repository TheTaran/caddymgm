What's Changed

## Bug Fixes

- Replace the CARTO basemap, which now requires an API key, with the free and keyless OpenFreeMap basemap.
- Preserve interactive zoom, pan, location markers, and OpenStreetMap attribution without Leaflet.
